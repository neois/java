 java 외부라이브러리 빌드
   
이클립스에서 외부 라이브러리를 Class Folder로 추가해서 빌드하면 따로 외부라이브러리로

추가 할 필요는 없다.

그러나 jar 파일로 제공되는 외부 라이브러리는 Libraries에 추가해서 사용하고 빌드시

프로젝트 오른쪽 클릭 시 나오는 "Export"를 선택 java>Runnable JAR File 선택

- launch configuration : 빌드 할 프로젝트 선택

- library handling : Extract requried .. 선택

빌드되면 한 개 jar에 모두 포함되어 따로 라이브러리 패스를 표시할 필요 없음
